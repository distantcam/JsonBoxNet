name: .NET Core

on: push

jobs:
  build:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v1    
    - name: Build with dotnet
      run: dotnet build --configuration Release
      working-directory: ./source
    - name: Run unit tests
      run: dotnet test
      working-directory: ./source
    - name: Build NuGet Package
      run: dotnet pack --include-symbols --configuration Release -o NuGetPackages
      working-directory: ./source
    - name: Create Release
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        files: |
          .\source\NuGetPackages\*.nupkg
          .\source\NuGetPackages\*.snupkg
    - name: Push NuGet Package
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      run: dotnet nuget push .\source\NuGetPackages\*.nupkg -k ${{ secrets.GITHUB_PACKAGE_KEY }} -s https://nuget.pkg.github.com/distantcam/index.json